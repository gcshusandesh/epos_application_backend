'use strict';

/**
 * guest-mode-pin controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::guest-mode-pin.guest-mode-pin');
